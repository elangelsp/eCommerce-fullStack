#Imagen de partida
FROM node:20
WORKDIR /app
# Instalar dependencias
COPY package*.json ./
RUN npm install
# Copiar el código fuente
COPY . .
# Exponer el puerto
EXPOSE 4000
# Comando de inicio
CMD ["npm", "start"]